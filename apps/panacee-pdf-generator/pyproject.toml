[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "panacee-pdf-generator"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pandas>=2.0",
    "reportlab>=4.0",
]

[tool.setuptools]
py-modules = []
